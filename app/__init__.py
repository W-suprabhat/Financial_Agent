"""Financial Extraction Agent."""

# Single source of truth. The API reports it and every workbook records it in the
# audit trail, so a delivered file can always be traced back to the build that made it.
__version__ = "2.0.0"
