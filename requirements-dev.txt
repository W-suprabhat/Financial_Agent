-r requirements.txt

pytest>=8.0.0
httpx>=0.27.0

# Only needed to regenerate the synthetic test PDFs in tests/fixtures
reportlab>=4.0.0
pillow>=10.0.0
pypdf>=4.0.0

# LangGraph Studio (visual graph + step debugger)
langgraph-cli[inmem]>=0.2.6
